const deploymentMessages = {
    INIT_PROJECT: 'Deployment started',
    HEROKU_CREATE: 'Project was created',
    GIT_REMOTE: 'Changes were made',
    GIT_ADD: 'Changes were saved',
    GIT_PUSH: 'Updating project'
};

export default deploymentMessages;
